interface:
  display_name: "Grok Deep Search"
  short_description: "Local Grok web search and deep research"
  default_prompt: "Use $grok-deep-search to search the web, check for prior art, and continue a Grok-backed research thread."
policy:
  allow_implicit_invocation: true